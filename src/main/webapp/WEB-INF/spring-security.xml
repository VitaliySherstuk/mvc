<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"

             xsi:schemaLocation="http://www.springframework.org/schema/beans
                    http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
                    http://www.springframework.org/schema/security
                    http://www.springframework.org/schema/security/spring-security-4.2.xsd">

    <http auto-config="true" use-expressions="true">

        <intercept-url pattern="/login" access="isAnonymous()"/>
        <intercept-url pattern="/**/ticketForEvent" access="hasRole('ROLE_ADMIN')"/>
        <intercept-url pattern="/**" access="hasRole('ROLE_USER')"/>
        <logout logout-success-url="/login" logout-url="/j_spring_security_logout" delete-cookies="JSESSIONID"/>
        <csrf disabled="true"/>

       <form-login
                login-page="/login"
                login-processing-url="/j_spring_security_check"
                username-parameter="j_username"
                password-parameter="j_password"
                default-target-url="/"
                authentication-failure-url="/login"
                always-use-default-target='true'
        />

        <!--<custom-filter position="LOGOUT_FILTER" ref="logoutFilter"/>-->
    </http>


    <authentication-manager>
        <authentication-provider>
            <user-service>
                <user name="user" password="user" authorities="ROLE_USER"/>
                <user name="admin" password="admin" authorities="ROLE_USER, ROLE_ADMIN"/>
            </user-service>
            <!--<password-encoder ref="encoder" />-->
        </authentication-provider>
    </authentication-manager>


    <beans:bean id="daoAuthenticationProvider" class="org.springframework.security.authentication.dao.DaoAuthenticationProvider">
         <!-- class="org.springframework.security.providers.dao.DaoAuthenticationProvider">-->
        <beans:property name="userDetailsService" ref="userDetailsServiceImpl"/>
        <!--<beans:property name="passwordEncoder" ref="encoder"/>-->
    </beans:bean>

    <beans:bean id="userDetailsServiceImpl" class="beans.services.UserDetailsServiceImpl"/>


</beans:beans>