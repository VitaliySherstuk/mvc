<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xsi:schemaLocation="
        http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
        http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-4.0.xsd
        http://www.springframework.org/schema/aop
        http://www.springframework.org/schema/aop/spring-aop-4.0.xsd">

    <tx:annotation-driven proxy-target-class="true" transaction-manager="transactionManager"/>

    <bean class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
        <property name="locations">
            <list>
                <value>classpath:db.properties</value>
                <value>classpath:strategies/booking.properties</value>
                <value>classpath:auditoriums/blueHall.properties</value>
                <value>classpath:auditoriums/redHall.properties</value>
                <value>classpath:auditoriums/yellowHall.properties</value>
                <value>classpath:strategies/strategies.properties</value>
                <value>classpath:aspects/aspects.properties</value>
            </list>
        </property>
    </bean>

    <bean id="dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
        <property name="driverClassName" value="${jdbc.driver}"/>
        <property name="url" value="${jdbc.url}"/>
        <property name="username" value="${jdbc.user}"/>
        <property name="password" value="${jdbc.password}"/>
    </bean>

    <bean id="sessionFactory" class="org.springframework.orm.hibernate5.LocalSessionFactoryBean">
        <property name="dataSource" ref="dataSource"/>
        <property name="packagesToScan"
                  value="beans.models"/>

        <property name="hibernateProperties">
            <value>
                hibernate.format_sql=false
                hibernate.dialect=org.hibernate.dialect.H2Dialect
                hibernate.hbm2ddl.auto=create
            </value>
        </property>
        <property name="mappingLocations">
            <list>
                <value>classpath:mappings/auditorium.hbm.xml</value>
                <value>classpath:mappings/event.hbm.xml</value>
                <value>classpath:mappings/ticket.hbm.xml</value>
                <value>classpath:mappings/user.hbm.xml</value>
                <value>classpath:mappings/booking.hbm.xml</value>
                <value>classpath:mappings/userAccount.hbm.xml</value>
            </list>
        </property>
    </bean>

    <aop:config>
        <aop:pointcut id="fooServiceOperation" expression="execution(* beans.services.BookingServiceImpl.*(..))"/>
        <aop:advisor advice-ref="companyServiceTxAdvice" pointcut-ref="fooServiceOperation"/>
    </aop:config>

    <tx:advice id="companyServiceTxAdvice" transaction-manager="transactionManager">
        <tx:attributes>
            <!-- set propogation required on create methods, all others are read-only -->
            <tx:method name="create*" propagation="REQUIRED"/>
            <tx:method name="update*" propagation="REQUIRED"/>
            <tx:method name="delete*" propagation="REQUIRED"/>
            <tx:method name="*" read-only="false" />
        </tx:attributes>
    </tx:advice>

    <bean id="transactionManager" class="org.springframework.orm.hibernate5.HibernateTransactionManager">
        <property name="sessionFactory" ref="sessionFactory"/>
    </bean>
</beans>